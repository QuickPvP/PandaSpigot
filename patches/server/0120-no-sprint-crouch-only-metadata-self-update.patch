From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: ItsSmile <smulelp@gmail.com>
Date: Wed, 7 Jan 2026 05:01:21 +0100
Subject: [PATCH] no-sprint-crouch-only-metadata-self-update


diff --git a/src/main/java/net/minecraft/server/EntityTrackerEntry.java b/src/main/java/net/minecraft/server/EntityTrackerEntry.java
index 0b90b6f30ea09fb117281d5ddd2fc752d2c139b5..5945a1662d51f9bfd49a3f756ff6e10b50a6a23d 100644
--- a/src/main/java/net/minecraft/server/EntityTrackerEntry.java
+++ b/src/main/java/net/minecraft/server/EntityTrackerEntry.java
@@ -16,6 +16,8 @@ import org.bukkit.event.player.PlayerVelocityEvent;
 public class EntityTrackerEntry {
 
     private static final Logger p = LogManager.getLogger();
+    private static final int SPRINTING_FLAG = 1 << 3;
+    private static final int CROUCHING_FLAG = 1 << 1;
     public Entity tracker;
     public int b;
     public int c;
@@ -39,6 +41,7 @@ public class EntityTrackerEntry {
     private boolean x;
     private boolean y;
     public boolean n;
+    private byte lastSelfEntityFlags;
     // PaperSpigot start
     // Replace trackedPlayers Set with a Map. The value is true until the player receives
     // their first update (which is forced to have absolute coordinates), false afterward.
@@ -58,6 +61,9 @@ public class EntityTrackerEntry {
         this.xRot = MathHelper.d(entity.pitch * 256.0F / 360.0F);
         this.i = MathHelper.d(entity.getHeadRotation() * 256.0F / 360.0F);
         this.y = entity.onGround;
+        if (entity instanceof EntityPlayer) {
+            this.lastSelfEntityFlags = entity.getDataWatcher().getByte(0);
+        }
     }
 
     public boolean equals(Object object) {
@@ -274,7 +280,43 @@ public class EntityTrackerEntry {
         DataWatcher datawatcher = this.tracker.getDataWatcher();
 
         if (datawatcher.a()) {
-            this.broadcastIncludingSelf(new PacketPlayOutEntityMetadata(this.tracker.getId(), datawatcher, false));
+            List<DataWatcher.WatchableObject> metadata = datawatcher.b();
+            if (metadata != null) {
+                PacketPlayOutEntityMetadata packet = new PacketPlayOutEntityMetadata(this.tracker.getId(), metadata);
+                boolean includeSelf = true;
+
+                if (this.tracker instanceof EntityPlayer) {
+                    DataWatcher.WatchableObject flagsObject = null;
+                    int newFlags = 0;
+                    int oldFlags = this.lastSelfEntityFlags & 0xFF;
+                    int diff = 0;
+
+                    for (DataWatcher.WatchableObject watchableobject : metadata) {
+                        if (watchableobject.a() == 0 && watchableobject.b() instanceof Byte) {
+                            flagsObject = watchableobject;
+                            break;
+                        }
+                    }
+
+                    if (flagsObject != null) {
+                        newFlags = (Byte) flagsObject.b() & 0xFF;
+                        diff = (newFlags ^ oldFlags) & 0xFF;
+
+                        if (metadata.size() == 1 && diff != 0 && (diff & ~(SPRINTING_FLAG | CROUCHING_FLAG)) == 0) {
+                            includeSelf = false;
+                        }
+
+                        this.lastSelfEntityFlags = (byte) newFlags;
+                    }
+
+                }
+
+                if (includeSelf) {
+                    this.broadcastIncludingSelf(packet);
+                } else {
+                    this.broadcast(packet);
+                }
+            }
         }
 
         if (this.tracker instanceof EntityLiving) {
diff --git a/src/main/java/net/minecraft/server/PacketPlayOutEntityMetadata.java b/src/main/java/net/minecraft/server/PacketPlayOutEntityMetadata.java
index 62492650a0e8d480da1b3920b7fe61ed5119749d..1a5f7997e20458355736cabd49a588a72e8cca42 100644
--- a/src/main/java/net/minecraft/server/PacketPlayOutEntityMetadata.java
+++ b/src/main/java/net/minecraft/server/PacketPlayOutEntityMetadata.java
@@ -10,6 +10,11 @@ public class PacketPlayOutEntityMetadata implements Packet<PacketListenerPlayOut
 
     public PacketPlayOutEntityMetadata() {}
 
+    public PacketPlayOutEntityMetadata(int i, List<DataWatcher.WatchableObject> list) {
+        this.a = i;
+        this.b = list;
+    }
+
     public PacketPlayOutEntityMetadata(int i, DataWatcher datawatcher, boolean flag) {
         this.a = i;
         if (flag) {
